// Estilo compartido para los <Tooltip> de Recharts, consistente con el diseño de las tarjetas.
export const chartTooltipStyle = {
  contentStyle: {
    background: 'var(--surface-card)',
    border: '1px solid var(--border-soft)',
    borderRadius: 12,
    boxShadow: 'var(--shadow-card)',
    padding: '8px 12px',
    fontFamily: 'inherit',
    fontSize: 12,
  },
  labelStyle: {
    color: 'var(--text-heading)',
    fontWeight: 700,
    marginBottom: 2,
  },
  itemStyle: {
    color: 'var(--text-muted)',
    fontSize: 12,
    padding: 0,
  },
}
